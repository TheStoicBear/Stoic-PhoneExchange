Config = {}

-- Server name or identifier for notifications
Config.Servername = "Your Server Name"

-- Notification colors (example colors)
Config.NotificationColors = {
    BackgroundColor = "#1a202c",
    BorderColor = "#4299e1",
    IconColor = "text-blue-500"
}

-- Dialog colors (example colors)
Config.DialogColors = {
    BackgroundColor = "#1a202c",
    BorderColor = "#4299e1",
    TextColor = "#ffffff"
}

-- Notification size (choose from 1 to 10)
Config.Size = 5 -- Default size, change as needed
Config.position = 'top-right' -- Set your default position here
-- Cooldown times in seconds
Config.RequestCooldown = 30 -- Cooldown for /reqnum command
Config.GiveNumberCooldown = 30 -- Cooldown for /givenum command
Config.ApproveRequestCooldown = 30 -- Cooldown for /apreqnum command

